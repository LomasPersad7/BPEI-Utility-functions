# Project Scaffolder

A Python CLI to automate creation of numbered project directories
with R scaffolding (`R/`, `data/`, `analysis/`, `sql/`).

## Install (local development)

```bash
pip install -e .